# ToMQA 数据集配置

dataset: ToMQA
path: ToMQA/test

# 默认 prompt 方法
method: zero_shot

# 结构化输出的 schema 类名（从 src.schemas 导入）
schema: OpenAnswer

# 系统 prompt（可选，会覆盖 experiment_config.yaml 中的设置）
system_prompt: ""
