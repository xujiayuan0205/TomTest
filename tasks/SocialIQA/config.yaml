# SocialIQA 数据集配置

dataset: SocialIQA
path: SocialIQA/test

# 默认 prompt 方法
method: zero_shot

# 结构化输出的 schema 类名（从 src.schemas 导入）
schema: MCQAnswer3

# 系统 prompt（可选，会覆盖 experiment_config.yaml 中的设置）
system_prompt: ""
